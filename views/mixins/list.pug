mixin list(component, entry, template)
	li.Component.drag.draggable-item(id= component.id)
		h2 Title 
		ul.listItems
			li.ListItem
				input.listItemInput(placeholder= "Write something")
		.addNewListItemButton + Add Item

