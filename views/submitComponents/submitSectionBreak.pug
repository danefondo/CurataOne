mixin submitSectionBreak(component)
	li.component__submitEntry.drag.draggable-item(id= "entry_" + component.id, data-component="sectionBreak" data-order= component.componentOrder)
		.sub-sortable-container
			.sectionBreakBlock Section Break Block

mixin submitSectionBreak(component)
	li.component__submitEntry.drag.draggable-item(id= component.id, data-component="sectionBreak" data-order= component.componentOrder)
		.sub-sortable-container
			.sectionBreakBlock Section Break Block